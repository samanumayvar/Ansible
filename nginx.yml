
---

* name: Install Nginx package
  hosts: frontend
  become: yes

  tasks:

  * name: Install nginx
    dnf:
    name: nginx
    state: present

  * name: Start and enable nginx
    service:
    name: nginx
    state: started
    enabled: yes
