- name: demo on variables
  hosts: localhost
  connection: local

  vars:
    course: "Ansible"
    trainer: "Sivakumar"
    duration: "150hrs"

  tasks:
  - name: display the variables
    vars:
     course: "Ansible Automation"
    debug:  
        msg: "Hi, I am learning {{ course }} variables, trainer is {{ trainer }}, Duration is {{ duration }}"